#!/usr/bin/python3
def uppercase(str):
    up = ""
    for i in str.split():
        if 97 <= ord(c) <= 122:
            up += chr(ord(str) - 32)
        else:
            result += c  # Leave it as it is
    print(result)