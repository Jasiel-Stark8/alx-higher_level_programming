#include <stdio.h>


int main() {
    int arr[10];
    printf("ENter your numbers: ");
    scanf("%d", &arr[10]);

    for (i = 0; i < arr; i++)
    {
        // sum = 0;
        // sum += arr[i];
        // return sum;
        printf("%d\n", arr[i]);
    }
}